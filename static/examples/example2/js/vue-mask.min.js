var mask_trigger=function trigger(el,type){var e=document.createEvent('HTMLEvents');e.initEvent(type,true,true);el.dispatchEvent(e);};Vue.directive('mask',{bind:function(el,binding,vnode){el._mask=binding.value;},componentUpdated:function(el){var previous_value=el._previous||'';if(el.value!=previous_value){var new_value='';var digits=el.value.replace(/[^0-9]*/ig,'');var mask=el._mask;for(var k=0,i=0;k<mask.length;k++){if(digits.length<=i)
break;else if(mask[k]=='#')
new_value+=digits[i++];else
new_value+=mask[k];}
el._previous=el.value=new_value;mask_trigger(el,'input');};}});
